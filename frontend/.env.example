# Only used by `npm run dev` (local Vite dev server). Points straight at the
# backend, bypassing nginx. Leave unset to use the default `/api` (works when
# running behind the bundled nginx reverse proxy in Docker / Render).
VITE_API_BASE_URL=http://localhost:8000/api
